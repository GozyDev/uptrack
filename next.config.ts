import type { NextConfig } from "next";

const nextConfig = {
  images: {
    domains: ["avatars.githubusercontent.com","lh3.googleusercontent.com"], // allow GitHub avatars
  },
};

export default nextConfig;
